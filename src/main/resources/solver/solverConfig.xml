<?xml version="1.0" encoding="UTF-8"?>
<solver xmlns="https://timefold.ai/xsd/solver"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://timefold.ai/xsd/solver https://timefold.ai/xsd/solver/solver.xsd">

    <solutionClass>dev.sylvain.planning.domain.PlanningFestival</solutionClass>
    <entityClass>dev.sylvain.planning.domain.PosteAffectation</entityClass>

    <scoreDirectorFactory>
        <constraintProviderClass>dev.sylvain.planning.solver.PlanningConstraintProvider</constraintProviderClass>
    </scoreDirectorFactory>

    <!--
        Phases intentionally left implicit so Timefold uses its default tuned
        pipeline (FIRST_FIT construction heuristic + Late Acceptance local
        search). Overriding either phase without a matching diversification of
        moves has empirically produced worse hard scores at short time
        budgets. Tune here only after measuring against ScoreAnalysis output.

        Termination (seconds / unimproved-seconds) is injected programmatically
        by PlanningService so that individual solve requests can override the
        default from application.properties.
    -->

</solver>
