<?xml version="1.0" encoding="UTF-8"?>
<solver xmlns="https://timefold.ai/xsd/solver"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://timefold.ai/xsd/solver https://timefold.ai/xsd/solver/solver.xsd">

    <solutionClass>dev.sylvain.planning.domain.PlanningFestival</solutionClass>
    <entityClass>dev.sylvain.planning.domain.PosteAffectation</entityClass>

    <scoreDirectorFactory>
        <constraintProviderClass>dev.sylvain.planning.solver.PlanningConstraintProvider</constraintProviderClass>
    </scoreDirectorFactory>

    <!--
        Explicit pipeline. FIRST_FIT quickly builds an initial assignment, then
        Late Acceptance does the heavy lifting. A dedicated union move selector
        (change + swap) keeps a healthy move diversity so the search can trade
        an "unavailable" assignment for an available animateur rather than
        stalling. Termination (seconds / unimproved-seconds) is injected
        programmatically by PlanningService.
    -->
    <constructionHeuristic>
        <constructionHeuristicType>FIRST_FIT</constructionHeuristicType>
    </constructionHeuristic>
    <localSearch>
        <unionMoveSelector>
            <changeMoveSelector/>
            <swapMoveSelector/>
        </unionMoveSelector>
        <acceptor>
            <lateAcceptanceSize>400</lateAcceptanceSize>
        </acceptor>
        <forager>
            <acceptedCountLimit>4</acceptedCountLimit>
        </forager>
    </localSearch>

</solver>
