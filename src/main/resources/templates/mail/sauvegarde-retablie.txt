La sauvegarde automatique de la base a de nouveau réussi le {tentative} ({fichier}), après {#if echecs > 1}{echecs} tentatives{#else}une tentative{/if} en échec.

Aucune action n'est attendue.{#if lien} Le compte rendu reste consultable sur l'écran Paramètres, onglet Globaux : {lien}{/if}
