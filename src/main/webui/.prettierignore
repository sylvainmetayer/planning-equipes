# The Angular templates are excluded on purpose: a blank between two inline
# elements is rendering, and prettier moves them. `npm run format` never
# passed them, but nothing stopped an editor's format-on-save or a bare
# `prettier --write .` from rewriting all 71 of them (see AGENTS.md).
*.html

# Generated, and shaped by `JSON.stringify` rather than by hand: reformatting it
# would only make `format-check` fail on the next build that regenerates it.
src/app/pages/nouveautes/news-data.ts
